第一種方法:將4-14行連結成是全放入def定義函式中

結論:其他定義函式(def simulation()、def velocity())會分別出現clientID定義的問題(也有嘗試在每個定義函式加入clientID的連結號)，但最終得到結果一樣是API程式跑完軟體中模擬與物件都沒動(簡單來說失敗了)


第二種方法:採用def定義函式參數來設定連結號(直接在呼叫函式打sim.simxStart('127.0.0.1',1997,true,true.5000,5)來設定連結)，BTW 已將其他定義函式中多新增一個參數以便設定連結號

結論:API啟動後程式跑完、軟體中模擬有但物體不動


第三種方法:將clientID放定義函式外(跟原先沒變動的時候一樣)

結論:啟動後模擬與物件會隨著設定跑