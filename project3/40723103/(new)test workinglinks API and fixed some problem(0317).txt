機構作動測試階段結果:

第一次測試結果:首先在軟體中將所有旋轉軸都勾選速度(速度零時機構不作動未勾選，因為勾選時啟用程式會卡)，而後啟動API發現連桿接轉軸會跑掉

第二次測試結果:之後看好跑掉的連桿將該旋轉軸取消速度，接下來個別測試兩三次去看桿件是否因為作動軸而跑掉，最終將機構調整至使用程式API才可轉動(原本檔案是按下模擬就會開始運作)。

設定測試時間:主要想自訂測試時間並在到達時間後自行停止運轉，目前測試到可以等待幾秒機構才會操作(但偏離原本目的)，後面也有著嘗試其他程式，不過還未debug(程式在定義函式階段就出現錯誤)